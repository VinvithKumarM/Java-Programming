package Test;

import java.util.ArrayList;
import java.util.List;

public class Test1 {
  //  List<Product> -> id, name, price

    //Count price
    //List<Product> productsList = new ArrayLisr();
    //List<Integer> pricesList = new ArrayList();
   //  sumOfPrices = productList.stream(a -> a.price).collecctions.toList();
     int sum = 0;
//     for(int i : pricesList){
//
//    }

    //List<integer> numbers = new ArrayList();

  //   numbers..filter(i -> i/2 == 0).;
    public void doSomeStuff(){
        List<Integer> evenNumbers = new ArrayList();
      //  evenNumbers.stream().forEach(i -> System.out::println(i));
    }

    


}
